baseURL = "https://hyegyeongseo.github.io/"
languageCode = "ko"
title = "기록 블로그"
theme = "PaperMod"
paginate = 10
timezone = "Asia/Seoul"

[outputs]
  home = ["HTML", "RSS", "JSON"]

[params]
  env = "production"
  description = "트러블슈팅 기록"
  author = "hyegyeongseo"
  defaultTheme = "auto"
  ShowReadingTime = false
  ShowShareButtons = false
  ShowPostNavLinks = true
  ShowBreadCrumbs = false
  ShowCodeCopyButtons = true
  ShowToc = true
  TocOpen = true

  [params.homeInfoParams]
    Title = "기록 블로그"
    Content = "시행착오를 기록합니다"

  [[params.socialIcons]]
    name = "github"
    url = "https://github.com/hyegyeongseo"

[menus]
  [[menus.main]]
    name = "Tags"
    url = "/tags/"
    weight = 1

[markup]
  [markup.highlight]
    # noClasses=false → chroma renders class names (.k, .nf, .s, ...) instead of inline styles,
    # so the syntax theme can be controlled from custom.css and switch between light/dark.
    noClasses = false
    # style is still used as a fallback for the few classes we don't override.
    style = "dracula"
    lineNos = false
    codeFences = true
    guessSyntax = true